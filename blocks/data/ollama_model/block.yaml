name: Ollama Model
type: ollama_model
category: data
description: Validate Ollama connection and return model reference
version: "1.0.0"
deprecated: true
deprecated_message: "Use Model Selector block instead. It handles all model sources (Ollama, MLX, HuggingFace, local)."

inputs: []

outputs:
  - id: model
    label: Model
    data_type: model

config:
  model_name:
    type: string
    label: Model Name
    default: "llama3.2"
  endpoint:
    type: string
    label: Endpoint URL
    default: "http://localhost:11434"
